class RunCommand {
    runCommand(input) {
        // Process Language
        // Pipe command output
        // Write -> send code block
        // Read -> Send audio
        // Nav -> Send nav command
    }
}
export default new RunCommand();